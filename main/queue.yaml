

total_storage_limit: 120M # Set the total storage limit for all queues to 120MB - this protects against fork-bomb or DOS attack


# free quota:- up to 10 queues (push and pull) + default queue

queue:
- name: mailSender
  rate: 1/m             # TODO: Change to 1/s
  bucket_size: 1
  max_concurrent_requests: 1
  
- name: SignupQ
  mode: pull
  retry_parameters:
    task_retry_limit: 7 # the system will delete the task after it has been leased this number of times
